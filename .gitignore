*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Model weights are gigabytes — never commit them
*.gguf
*.task
models/

# Signing keys never belong in the repository. The one still tracked here is
# being moved into a repository secret and removed from history before this
# repo is made public.
*.keystore
*.jks
*.p12
keystore.properties
