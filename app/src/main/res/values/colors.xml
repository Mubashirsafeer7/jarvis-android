<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The launcher foreground is the reference artwork itself, and the artwork
         carries its own near-black ground. Matching that exactly (rather than the
         app's #060B14 navy) is what stops a faint square seam showing inside the
         launcher's mask. -->
    <color name="ic_launcher_background">#000106</color>
</resources>
